import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Successful Execution Should Leave Proof.",
  description: "ILAIOS is being designed around evidence-backed validation so meaningful outcomes can be inspected instead of accepted only because an agent reported success.",
  alternates: {
    canonical: "/platform/evidence",
    languages: {
      en: "/platform/evidence",
      tr: "/tr/platform/evidence",
      "x-default": "/platform/evidence"
    }
  },
};
