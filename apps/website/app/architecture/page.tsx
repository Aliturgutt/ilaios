import type { Metadata } from "next";
import Link from "next/link";

export const metadata: Metadata={title:"Architecture",description:"Understand the ILAIOS control-plane architecture, authority boundaries, execution model, and evidence path.",alternates:{canonical:"/architecture",languages:{en:"/architecture",tr:"/tr/architecture","x-default":"/architecture"}}};

const layers=[["01","Clients","Desktop, mobile, and web interfaces present controlled projections of system state and collect goals, approvals, and user decisions."],["02","Control plane","Backend authority owns policy, authorization, tenant context, orchestration, state transitions, and critical decisions."],["03","Policy & approvals","Permissions, approval requirements, and execution conditions are evaluated before sensitive work proceeds."],["04","Execution & tools","Deterministic steps are preferred; intelligent capabilities operate through bounded task and tool contracts."],["05","Validation & evidence","Results are checked and durable evidence records support review, audit context, recovery, and accountable delivery."]] as const;
const boundaries=[["Authority boundary","A client or agent may request work, but the request does not become permission simply because it was generated successfully."],["Execution boundary","Tools and services receive only the scope needed for an approved job rather than inheriting unrestricted ambient authority."],["Validation boundary","Completion is determined by explicit checks and acceptance criteria where possible, not by model confidence alone."],["Evidence boundary","Meaningful execution events remain reconstructable so operators can understand what happened and why a result was accepted."]] as const;
const designQuestions=[["Who is authoritative?","Backend/control-plane services remain the source of truth for security- and product-critical runtime decisions."],["Where does intelligence fit?","Models and agents are capabilities inside governed workflows; they can assist planning, interpretation, and execution without owning global authority."],["How is change contained?","Durable contracts separate clients, control logic, execution services, and evidence so one presentation surface can evolve without redefining system trust."],["How does failure behave?","Critical paths are designed to stop, escalate, or remain pending when required policy, approval, validation, or evidence conditions are not satisfied."]] as const;

export default function Page(){return <>
  <section className="shell page-hero"><div className="eyebrow">Architecture</div><h1>Authority lives in the control plane.</h1><p className="lead">ILAIOS separates user interfaces, intelligent capabilities, and execution tools from the backend authority that governs policy, authorization, validation, state, and evidence.</p></section>
  <section className="section architecture-section"><div className="shell"><div className="section-heading"><div><div className="eyebrow">System model</div><h2>A layered path from intent to evidence.</h2></div><p className="muted">Each boundary exists to keep presentation, intelligence, execution, and authority from collapsing into one opaque component.</p></div><div className="control-stack">{layers.map(([n,t,x],i)=><div key={t}>{i>0&&<div className="stack-connector">governed boundary ↓</div>}<div className={`stack-layer ${i===1?"authority-layer":""}`}><span>{n}</span><strong>{t}</strong><small>{x}</small></div></div>)}</div></div></section>
  <section className="section"><div className="shell"><div className="section-heading"><div><div className="eyebrow">Trust boundaries</div><h2>Separate permission from capability.</h2></div><p className="muted">Being able to produce a plan, call a model, or access a tool is not the same as having authority to create a side effect.</p></div><div className="grid two-up">{boundaries.map(([t,x])=><article className="card" key={t}><h3>{t}</h3><p>{x}</p></article>)}</div></div></section>
  <section className="section architecture-section"><div className="shell"><div className="eyebrow">Architecture questions</div><h2>Four questions define the operating model.</h2><div className="grid two-up">{designQuestions.map(([t,x])=><article className="card" key={t}><h3>{t}</h3><p>{x}</p></article>)}</div></div></section>
  <section className="section"><div className="shell split-copy"><div><div className="eyebrow">Core rule</div><h2>Clients are projections, not system authority.</h2></div><div><p className="lead small">Security and product-critical decisions are not delegated to a desktop or mobile interface merely because the interface can display or request them.</p><p className="muted">This separation lets clients evolve while policy, execution, validation, and evidence contracts remain durable behind them.</p><div className="actions"><Link className="button secondary" href="/platform/control-plane">Control plane</Link><Link className="button secondary" href="/security">Security model</Link><Link className="text-link" href="/docs">Documentation →</Link></div></div></div></section>
</>}
