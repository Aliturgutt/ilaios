import type { Metadata } from "next";
import Link from "next/link";
export const metadata: Metadata={title:"Documentation",description:"Read the public ILAIOS architecture, security, execution, agent, and evidence documentation overview.",alternates:{canonical:"/docs",languages:{en:"/docs",tr:"/tr/docs","x-default":"/docs"}}};
const docs=[["Getting started","Start with the system model: clients request and display work; backend/control-plane authority decides what is allowed and what state is authoritative."],["Architecture","Understand the separation between clients, control plane, policy, execution tools, validation, and evidence."],["Security model","Review permission boundaries, approval gates, least-privilege execution, and auditable state transitions."],["Execution model","Prefer deterministic execution when available; use intelligent capabilities inside explicit task and tool contracts."],["Agent model","Agents are bounded capabilities with defined roles, permissions, inputs, outputs, dependencies, and escalation paths."],["Evidence model","Important actions should produce durable records that allow validation, review, and recovery without relying on narrative memory."]] as const;
export default function Page(){return <><section className="shell page-hero"><div className="eyebrow">Documentation</div><h1>Public documentation for the ILAIOS system model.</h1><p className="lead">This documentation overview covers stable architectural principles that can be described publicly today. API references and release-specific integration instructions will be published only when the corresponding interfaces are ready and verified.</p></section><section className="section"><div className="shell"><div className="grid two-up">{docs.map(([t,x])=><article className="card" key={t}><h3>{t}</h3><p>{x}</p></article>)}</div><div className="actions"><Link className="button secondary" href="/architecture">Architecture</Link><Link className="button secondary" href="/security">Security</Link><Link className="button secondary" href="/trust">Trust Center</Link></div></div></section></>}
