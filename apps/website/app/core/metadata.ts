import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "One Control Authority Around Every Governed Execution.",
  description: "Core connects the requested outcome to permissions, bounded execution, verification, evidence and recovery without handing system authority to a model, agent or provider.",
  alternates: {
    canonical: "/core",
    languages: {
      en: "/core",
      tr: "/tr/core",
      "x-default": "/core"
    }
  },
};
