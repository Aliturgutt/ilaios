import "../factory-detail.css";
import type { ReactNode } from "react";
export default function Layout({ children }: { children: ReactNode }) { return <div className="factory-route">{children}</div>; }
