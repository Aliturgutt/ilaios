import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Important Actions Should Remain Inspectable.",
  description: "ILAIOS is being designed to retain meaningful execution context so sensitive operations can be reviewed after the fact instead of disappearing into opaque agent behavior.",
  alternates: {
    canonical: "/security/audit",
    languages: {
      en: "/security/audit",
      tr: "/tr/security/audit",
      "x-default": "/security/audit"
    }
  },
};
