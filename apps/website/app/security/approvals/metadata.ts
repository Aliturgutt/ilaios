import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Human Authority Stays Human Where Required.",
  description: "ILAIOS is being designed so operations that require a human decision can pause at explicit approval gates instead of being silently delegated to autonomous execution.",
  alternates: {
    canonical: "/security/approvals",
    languages: {
      en: "/security/approvals",
      tr: "/tr/security/approvals",
      "x-default": "/security/approvals"
    }
  },
};
