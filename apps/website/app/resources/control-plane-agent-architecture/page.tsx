import type { Metadata } from "next";
import Link from "next/link";
const siteUrl=process.env.NEXT_PUBLIC_SITE_URL??"https://ilaios.com"; const url=`${siteUrl}/resources/control-plane-agent-architecture`;
export const metadata:Metadata={title:"Control-Plane Agent Architecture",description:"Why agent systems should keep policy, authorization, state, validation, and audit authority in a durable control plane rather than in clients or model outputs.",alternates:{canonical:"/resources/control-plane-agent-architecture",languages:{en:"/resources/control-plane-agent-architecture",tr:"/tr/resources/control-plane-agent-architecture","x-default":"/resources/control-plane-agent-architecture"}}};
const jsonLd={"@context":"https://schema.org","@type":"Article",headline:"Control-Plane Agent Architecture",datePublished:"2026-08-11",dateModified:"2026-08-11",author:{"@id":`${siteUrl}/about#founder`},publisher:{"@id":`${siteUrl}/#organization`},mainEntityOfPage:url};
export default function Page(){return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><article><section className="shell page-hero"><div className="eyebrow">Engineering Insight</div><h1>Agent architecture needs a durable source of authority.</h1><p className="lead">When clients, agents, and models can all influence a workflow, the system needs one place where policy, authorization, state transitions, and acceptance remain authoritative.</p></section><section className="section"><div className="shell split-copy"><div><h2>The control plane owns decisions that must survive model variability.</h2></div><div><p>Models are probabilistic and clients are replaceable presentation surfaces. Neither should silently become the system of record for permissions, tenant boundaries, durable job state, or release decisions.</p><p>A control-plane architecture centralizes identity context, policy, workflow state, capability contracts, approvals, validation, evidence, and recovery rules behind stable interfaces.</p><h2>Agents become bounded capabilities.</h2><p>Specialized agents can still be powerful. Their role is clearer when they operate through explicit inputs, outputs, permissions, allowed callers, allowed targets, and escalation paths instead of relying on human-readable names or prompt conventions for authority.</p><h2>Clients remain projections.</h2><p>Desktop, mobile, and web clients can evolve independently when they consume authoritative backend state rather than recreate policy locally.</p><div className="actions"><Link className="text-link" href="/architecture">Architecture →</Link><Link className="text-link" href="/desktop">Desktop client →</Link></div></div></div></section></article></>}
