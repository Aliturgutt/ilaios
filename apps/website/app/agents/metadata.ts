import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Specialist Roles Behind Stable Identities, Explicit Permissions And Independent Verification.",
  description: "ILAIOS uses a governed multi-team agent organization, but orchestration does not depend on a name or persona. Machine IDs, capability contracts, permissions, allowed callers and targets, escalation paths and verifier identities remain authoritative.",
  alternates: {
    canonical: "/agents",
    languages: {
      en: "/agents",
      tr: "/tr/agents",
      "x-default": "/agents"
    }
  },
};
