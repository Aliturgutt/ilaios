# ILAIOS Canonical Publication Manifest — 2026-08-13

This migration publishes the frozen 19-item canonical documentation set: 18 primary Markdown documents plus the canonical `docs/adr/` decision-record directory.

The prior canonical architecture and prior ADR namespace are preserved under `docs/archive/pre-2026-08-13/` for history only. They are not current architecture authority.

Current authority is established by the published canonical paths and the repository documentation index. Implementation, test, CI, runtime, and deployment evidence remain the source of current-state truth.

The migration branch completed pre-PR checks for canonical file presence, ADR completeness, relative-link resolution, active-authority duplication, staging-artifact cleanup, high-signal secret-value patterns, and `git diff --check`.
