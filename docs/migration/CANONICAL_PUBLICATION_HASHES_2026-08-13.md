# Canonical Publication Hash Policy — 2026-08-13

The final publication is hash-verified after merge against the exact repository files on `master`. The pre-migration locked archive remains historical input evidence. Any explicitly documented, semantics-preserving GitHub publication normalization receives a new publication hash rather than reusing the pre-migration archive hash.
