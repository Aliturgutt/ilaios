# Canonical Documentation Directory

Status: NAVIGATION ONLY — NOT AN INDEPENDENT AUTHORITY

The primary architecture authority is `SYSTEM_ARCHITECTURE.md`. This directory contains the architecture/product/implementation canonical subset. Other scoped canonical documents live under `docs/governance/`, `docs/security/`, `docs/operations/`, and `docs/adr/`.

Use `../../README.md` and `../DOCUMENTATION_INDEX.md` for the complete 19-item map.
