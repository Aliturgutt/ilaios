[project]
name = "ILAIOS"
dynamic = ["version"]
dependencies = [
    "requests>=2.32,<3",
    "python-dotenv>=1.0,<2",
]

[tool.mypy]
mypy_path = "."
explicit_package_bases = true
strict = true

[[tool.mypy.overrides]]
module = "requests.*"
ignore_missing_imports = true
