# Canonical Contracts

`ilaios_contracts` owns versioned provider-neutral boundary envelopes, the accepted job lifecycle alias, and the release-state contract. Compatibility fails closed on unsupported versions. It does not introduce a second job lifecycle.
