schema_version: "1.0"
state: REFERENCES_ONLY
contains_secret_values: false
secret_delivery:
  backend: null
  workload_identity: null
  recommended_open_source_bridge: external-secrets-operator
  rotation_owner: null
required_references:
  - id: database_application_credentials
    scope: per_environment
    creator: human_or_approved_provider_workflow
  - id: queue_application_credentials
    scope: per_environment
    creator: human_or_approved_provider_workflow
  - id: object_store_application_credentials
    scope: per_environment
    creator: human_or_approved_provider_workflow
  - id: evidence_store_signing_key
    scope: per_environment
    creator: approved_kms_or_hsm_workflow
  - id: session_and_api_signing_key
    scope: per_environment
    creator: approved_kms_or_hsm_workflow
  - id: observability_export_credentials
    scope: per_environment
    creator: human_or_approved_provider_workflow
  - id: container_registry_pull_identity
    scope: per_environment
    creator: approved_workload_identity_workflow
  - id: dns_automation_credentials
    scope: canary_zone_only
    creator: human_or_approved_provider_workflow
  - id: tls_automation_credentials
    scope: canary_issuer_only
    creator: human_or_approved_provider_workflow
  - id: external_ai_and_media_provider_credentials
    scope: provider_and_environment_specific
    creator: human_after_provider_selection
controls:
  plaintext_repository_storage_prohibited: true
  personal_credentials_prohibited: true
  environment_isolation_required: true
  rotation_and_revocation_test_required: true
  secret_access_audit_required: true
