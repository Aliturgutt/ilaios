# sf-security-review

Identity: `sf-security-review` v1.0.0, IMPLEMENTED, independent-verification, critical risk.

Purpose: independently review security impact and reject unproven/bypassing change proposals. Inputs: `intent`, `changed_paths`, `change_evidence`. Outputs: evidence-backed findings, decision, evidence references.

Specialization: review trust boundaries, policy bypass, secret/network exposure, unsafe dependencies, data/security regressions and production paths. Generated work cannot self-certify; independent review is mandatory.

The common `../CONTRACT.md` applies.
