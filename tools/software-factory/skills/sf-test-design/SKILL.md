# sf-test-design

Identity: `sf-test-design` v1.0.0, IMPLEMENTED, test-quality.

Purpose: derive a risk-based validation matrix. Inputs: `intent`, `changed_paths`, `risk_flags`. Outputs: validation matrix, risk-to-test mapping, unit/integration/contract/E2E layers, coverage gaps.

Specialization: design tests against behavior and risk, not implementation trivia; consume SF-5 impact evidence and declare the SF-6 runtime adapters needed for validation.

The common `../CONTRACT.md` applies.
