# sf-backend-engineering

Identity: `sf-backend-engineering` v1.0.0, IMPLEMENTED, engineering.

Purpose: propose bounded services/API/backend ChangeSets. Inputs: `intent`, `changed_paths`. Outputs: change proposal, tests, evidence, unresolved findings.

Specialization: preserve service boundaries, API/data contracts and governance; use canonical repository intelligence and Python runtime validation. No direct master/production mutation. Independent review is required.

The common `../CONTRACT.md` supplies shared preconditions, deny-set, evidence, failure and completion gates.
