# sf-test-generation

Identity: `sf-test-generation` v1.0.0, IMPLEMENTED, test-quality.

Purpose: propose tests for actual changed behavior. Inputs: `intent`, `changed_paths`, `validation_matrix`. Outputs: change proposal, tests, evidence, unresolved findings.

Specialization: include negative/adversarial cases where relevant and avoid tests that merely mirror implementation details. Canonical SF-5 impact and SF-6 runtime evidence govern scope.

The common `../CONTRACT.md` applies.
